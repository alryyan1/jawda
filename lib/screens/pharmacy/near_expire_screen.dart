import 'package:flutter/material.dart';

class NearExpireScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Near Expire Items'),
      ),
      body: Center(
        child: Text('Near Expire Items Page Content'),
      ),
    );
  }
}