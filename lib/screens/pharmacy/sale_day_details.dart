import 'package:flutter/material.dart';

class SaleDayDetails extends StatelessWidget {
  const SaleDayDetails({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}